spark_version: "4.2.0"
hadoop_version: "hadoop3"
spark_archive: "spark-{{ spark_version }}-bin-{{ hadoop_version }}.tgz"
spark_download_url: "https://archive.apache.org/dist/spark/spark-{{ spark_version }}/{{ spark_archive }}"
# spark_home lives in ../../group_vars/all.yml — shared with roles/spark_cluster
